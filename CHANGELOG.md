# 变更日志

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增
- A30b 批次：EPUB 替换规则接入（真机验证反馈）
  - 用户替换规则（净化规则）此前仅对 TXT 生效，EPUB 结构化管线未接入——现已在 Rust 块级应用，与 TXT 同口径
  - 规则哈希入 EPUB 分页缓存键，改规则即时生效换键重算；规则改写文本后命中块降级为整块统一样式（防富文本区间错位）
  - 搜索与展示在同函数同时机应用规则：搜索命中「规则后文本」，锚点落页含命中词（新增 2 个对齐单测锁死）

### 修复
- A30b：EPUB 搜索输入时阅读内容闪现刷新一次——软键盘开/关动画每帧触发全章重排（真机日志实锤 15+ 次）。修复：搜索对话框存活期间**显式冻结**视口尺寸处理（查找只是查找，只有点击结果才切换内容）+ 尺寸变化 200ms 防抖（动画中间值互相覆盖，最终值与原值一致则零重排）
- A30b：键盘弹出时应用内容被整体上推——manifest adjustResize 导致 Android 窗口随键盘缩小。改 adjustNothing（键盘悬浮覆盖），阅读器 Scaffold 恒定全高；输入对话框不做任何基于 viewInsets 的布局（本设备 insets 上报不可靠，会报≈整屏高度把对话框挤扁），搜索提交后主动收起键盘使结果完整可见
- A30b：搜索/书签跨章跳转先切章再加载导致旧章内容闪现一帧——章节随页面在 FFI 返回后一次性提交（deferred commit）
- 修复两个 bench example 缺失 LayoutConfig 新字段的历史编译错误

### 说明
- A30 批次：书内全文搜索
  - 菜单栏「搜索」入口：输入关键词全书搜索，结果列表显示章节与摘录（命中词高亮）
  - 点击结果跳转到命中位置（复用书签的章节+字符锚点机制）
  - TXT/EPUB 双格式统一体验，简繁转换状态下搜索与展示文本同口径
  - 搜索计算全在 Rust 线程池执行（单次异步 FFI），UI 线程零堵塞
  - 内置预算：命中上限 200 条 / 扫描超 5s 自动截断

## [1.0.2] - 2026-09-07

### 优化
- A29 批次：翻页手势接管——连续快翻不再"不跟手"
  - 动画在途时的新拖拽/点击手势：快进当前动画到终点并立即提交（原为整段排队等待 600-1100ms）
  - 新手势经发布重试链路启动（实测提交 10-90ms），拖拽尽快恢复跟手
  - 快进提交输入在接管时捕获，规避异步恢复期字段覆写竞态；提交在途时 end-during-pending 分支不再用旧帧开新动画
  - A29b：接管提交后复位活跃态，修复快速点击翻页偶发"闪一下"（pending 400ms 超时直翻）

### 修复
- A28 批次：图片就绪重绘修复 + 快照完整性失效机制（真机验证反馈）
  - 图片解码完成后页面下一帧自动重绘，无需翻页才显示（预热路径回调多播修复）
  - 翻页动画折叠层不再复用含灰色占位框的陈旧快照（依赖就绪即失效重建）

### 性能
- A27 批次：图片加载三阶段优化（详见 `docs/design/IMAGE_LOADING_PERFORMANCE.md`）
  - 首屏图片预热：打开书籍后立即预热当前页图片，延迟从 200-500ms 降至 <50ms
  - 解码并发提升：同时最多 2 个 → 4 个「取字节 + 解码」任务
  - Rust ResourceCache 扩容：50 → 150 张，减少图片密集书的 LRU 淘汰
  - 智能预测预热：连续同向翻页 ≥2 次时预热"下下页"，连续翻页零感知延迟
  - EPUB 分页缓存添加 900s TTL（与 TXT 对齐），长时间静读后陈旧缓存自动失效
  - 图片占位框状态化：加载中显示圆弧指示器、失败显示 × 标记、未请求纯灰块

### 新增
- 阅读器诊断日志：文件记录 + 菜单栏「日志导出」按钮（FilePicker SAF 导出，无需存储权限）

## [1.0.1] - 2026-09-05

### 新增
- 关于页面：显示应用名称、版本号、构建号、功能特性列表
- 版本管理基础设施：CHANGELOG.md + package_info_plus 集成
- A26 批次：图文混排分页算法优化（Text 段落与 Image/Table 协同决策）
- A25 批次：统一 EPUB/TXT 行级分页精度
- 页面填充率滑杆对 EPUB/TXT 双类型真实生效
- 页面溢出时的内容截断逻辑优化
- 极端情况警告日志（超高图片、长表格）

### 修复
- 页面溢出保护（页面内容超出底界时的截断处理）
- 孤寡行保护的浮点容差修复（0.5px 容差防止精度漂移导致的分页碎片）
- 图文混排页底留白不统一问题（通过前瞻机制预留空间）

### 变更
- 优化 APK 构建脚本（支持分 ABI 打包，`build_apk.ps1 -Abis arm64-v8a`）
- `page_fill_threshold` 语义重定义：从"EPUB 整段推页门槛"改为"内容区利用率"（0.50-1.00，对 EPUB/TXT 双类型统一生效）
- ARCHITECTURE.md 明确 ADR 编号（A1-A26）与用户版本号独立

## [1.0.0] - 2026-09-05

### 新增
- 初始版本
- EPUB 与 TXT 阅读支持
- 自定义字体、字号、行距
- 分页填充率调节
- 书签与阅读进度保存
- 章节导航
- Rust + Flutter 跨平台架构（book_parser、layout_engine、reader_core）
