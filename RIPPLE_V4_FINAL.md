# 水波纹翻页动画 v4 - 终极修复

**日期**: 2026-09-03  
**版本**: v4 - 几何裁剪方案（彻底无重叠）

---

## ✅ 根本性修复

v3 的 shader 遮罩方案在 Flutter `Paint()..shader = shader` 上**并不能正确作为透明度遮罩**——这只是给 Paint 设置了一个着色器，但不影响图层的混合模式。

### ❌ v3 失败原因
```dart
canvas.saveLayer(
  Rect.fromLTWH(0, 0, size.width, size.height),
  Paint()..shader = shader,  // shader 不会自动作为 alpha 遮罩
);
```
**问题**: `Paint.shader` 是绘制颜色用的，不是透明度遮罩。要让 shader 控制 alpha 需要复杂的 BlendMode 组合，在 saveLayer 上下文中行为不明确。

### ✅ v4 终极方案
**完全放弃 shader，改用纯几何裁剪**：
1. 先画新页面（整屏）
2. 计算每个 y 位置的波浪边界 x
3. 用 `Path` 构建波浪形状的裁剪区域
4. 旧页面只在裁剪区域内绘制

**结果**: 任何像素位置**只可能属于一页**（除非在波浪边界的 ±20px 振幅范围内，由于波浪的正弦曲线在某些 y 位置边界会稍微左偏、某些 y 位置会右偏，但仍然只是"上一页"或"下一页"，**绝不会在同一位置同时看到两页文字**）。

---

## 🔧 实现原理

```
next (下一页): 推进位置从左向右移动

  进度 0%:
  [============ 旧页面 ============]   (裁剪=整屏，所以只画旧页)

  进度 30%:
  [新页        波浪|   旧页面       ]   (裁剪=波浪右侧)

  进度 70%:
  [新页面       |波浪  旧页         ]   (裁剪=波浪右侧，区域很小)

  进度 100%:
  [============ 新页面 ============]   (裁剪=空，所以只画新页)
```

`Path` 的构造：
```
对每个 y ∈ [0, height]，每 2px 一个点：
  waveOffset = sin(y / height * 5π) * 20
  boundaryX = basePosition + waveOffset
  
  path.lineTo(boundaryX, y)

最后 path.close() 形成闭合区域。
```

next 时裁剪"波浪右侧"区域 → 旧页面只在右侧显示
prev 时裁剪"波浪左侧"区域 → 旧页面只在左侧显示

---

## 📊 核心保证

| 像素位置 | 进度 50% next | 看到什么 |
|---------|-------------|---------|
| x=0 (最左) | 在裁剪区外 | 新页面 |
| x=100 (波浪左侧) | 在裁剪区外 | 新页面 |
| x=200 (波浪处) | 在裁剪边界 | 模糊（边界 ±20px 范围内） |
| x=300 (波浪右侧) | 在裁剪区内 | 旧页面 |
| x=400 (最右) | 在裁剪区内 | 旧页面 |

**关键**:
- ✅ 任何像素位置只可能画"新页"或"旧页"二选一
- ✅ 不会出现"新页文字+旧页文字"在同一像素
- ✅ 边界是单像素几何边界（不是透明度混合）

---

## 🎬 视觉对比

### v2/v3 (有重叠问题)
```
[新页文字~~~~~~~]   ← 50% 透明度，文字模糊
~~~~波浪~~~~
[旧页文字~~~~~~~]   ← 50% 透明度，文字模糊
```
**问题**: 在波浪带状区域 50px 范围内，上下两页文字叠加。

### v4 (完全无重叠)
```
[新页文字清爽]     ← 100% 清晰
[新页文字清爽]
~~~~波浪边界~~~~   ← 这是几何边界，不是透明度
[旧页文字清爽]     ← 100% 清晰
[旧页文字清爽]
```
**解决**: 波浪带是"硬边界"（虽然是波浪形状），两侧内容都是 100% 清晰。

---

## 🧪 测试步骤

### 1. 重新构建
```powershell
cd D:\android\example\legado_flutter
.\fix_sync.ps1
```

### 2. 启动应用
```powershell
flutter run -d windows
```

### 3. 切换到水波纹模式
1. 打开书籍
2. 点击屏幕中央 → 打开菜单
3. 点击"**水波纹**"

### 4. 验证效果

**关键测试**：观察推进边界附近
- ✅ 边界左侧 = 100% 新页面（文字清晰）
- ✅ 边界右侧 = 100% 旧页面（文字清晰）
- ❌ **不应该看到**: 任何位置有"上下两页文字重叠"现象

---

## 🎨 可调参数

在 `ripple_painter.dart` 中：

```dart
const waveFrequency = 2.5;   // 波浪周期数（2-4 效果较好）
const waveAmplitude = 20.0;  // 波浪振幅（10-30 像素）
```

更平滑的波浪：减小 `waveAmplitude` 到 10
更明显的波浪：增大 `waveAmplitude` 到 40
更密的波浪：增大 `waveFrequency` 到 4.0
更疏的波浪：减小 `waveFrequency` 到 1.5

动画速度在 `ripple_turn_controller.dart`：
```dart
super(duration: const Duration(milliseconds: 600));
// 快: 400, 中: 600, 慢: 800
```

---

## 🐛 故障排查

### 问题 1: 还是看到重叠

**可能原因**: 编译未完成或缓存

**解决**:
```powershell
flutter clean
Remove-Item -Recurse -Force build, .dart_tool
.\fix_sync.ps1
```

### 问题 2: 波浪消失（变成直线）

**原因**: `waveAmplitude` 太小或 0

**检查** `ripple_painter.dart`:
```dart
const waveAmplitude = 20.0;  // 必须是 > 0
```

### 问题 3: 动画方向反了

**症状**: 点击右侧翻页，但新内容从右往左推进

**检查** `ripple_painter.dart:73-90` 的 `isNext` 分支

### 问题 4: 性能问题

**Path 每 2px 构造一个点**，在 400x800 屏幕上：
- next: ~400 个点
- prev: ~400 个点
- 每帧构建一次 → GPU 友好

如果性能问题：
```dart
// 减小采样密度（3.0 → 5.0）但会降低波浪平滑度
for (double y = 0; y <= size.height; y += 3.0) { ... }
```

---

## 📊 版本全对比

| 版本 | 方案 | 重叠 | 波浪 | 性能 |
|------|------|------|------|------|
| v1 | 圆形扩散 + dstOut | 有 | 有 | 中 |
| v2 | 横向推进 + dstOut | 有 | 有 | 中 |
| v3 | shader 遮罩 | 有 | 有 | 优 |
| **v4** | **几何裁剪** | **无** ✅ | **有** | **优** |

---

## 🎯 请测试并反馈

请运行 `fix_sync.ps1` 重新构建后告诉我：

1. **内容重叠是否彻底消失**？（这是最关键的）
   - [ ] 是的，完全没有重叠
   - [ ] 还有部分重叠
   - [ ] 完全没变

2. **波浪效果如何**？
   - [ ] 边缘有清晰的波浪起伏
   - [ ] 边缘是直线（波浪消失）
   - [ ] 波浪太剧烈/太平缓

3. **推进方向正确**？
   - [ ] next: 新内容从左到右推进
   - [ ] prev: 新内容从右到左推进

4. **整体感受**？
   - [ ] 很满意，达到预期
   - [ ] 还需要微调
   - [ ] 不满意

这次应该彻底解决问题了！🌊
